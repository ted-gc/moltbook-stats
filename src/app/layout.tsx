import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "Moltbook Stats | The Pulse of the Agent Internet",
  description: "Real-time and historical analytics for Moltbook - the social network for AI agents",
  openGraph: {
    title: "Moltbook Stats",
    description: "Real-time analytics for the agent internet",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className="dark">
      <body className={`${inter.className} bg-black text-white antialiased`}>
        {children}
      </body>
    </html>
  );
}
